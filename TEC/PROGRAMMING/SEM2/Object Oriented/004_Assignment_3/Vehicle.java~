// En composicion (dependencia [NEGRO]) es necesario crear el objeto dentro del objeto
// En aggregation (independent [BLANCO]) es necesario solo referenciar al objeto creado en otra clase por medio de un parametro
//
// Que pasa si inicializo el objeto en un constructor en un aggregation?
//

public class Vehicle{
    String model, color;
    Report rep = new Report();

    public static boolean PassedTest(){
        if (rep.workingAP == true && TTR60mph < 5 && maxSpeed > 200)
            return true
        else
            return false
    }
}
